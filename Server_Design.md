# תכנון צד השרת – מעבר לתמיכה בהרבה משתמשים

הרעיון של המסמך הזה פשוט: היום יש לנו שרת אחד (`python -m server`) שמחזיק את כל
המשחקים בזיכרון ומזיז את השעון בעצמו. זה עובד מצוין לכמה עשרות שחקנים, אבל נשבר
ברגע שרוצים הרבה. כאן אני עובר רכיב-רכיב על מה שצריך להוסיף, למה הוא בכלל נחוץ,
ואיך משתלבים אותו במה שכבר בנינו – בלי לזרוק הכל ולהתחיל מהתחלה.

הכלל שחוזר על עצמו לאורך כל המסמך: **בכל מקום שיש "אחד" – שוברים אותו להרבה
זהים.** ברגע שמפנימים את זה, רוב ההחלטות כאן מסתדרות מעצמן.

---

## במה אנחנו משתמשים – במבט מהיר

לפני שנכנסים לפרטים, הנה כל מה שנכנס לתמונה, כל אחד למה שהוא טוב בו:

- **Docker** – לארוז את השרת לחבילה אחת שרצה אותו דבר בכל מקום.
- **Kubernetes** – לנהל את כל הקונטיינרים: להריץ, לרפא, ולהתאים את הכמות לעומס.
- **Load Balancer** – דלת כניסה אחת שמפזרת כל שחקן לשרת פנוי.
- **Redis** – זיכרון משותף מהיר: מי מחובר, איזה חדר על איזה שרת, ותור ההמתנה.
- **PostgreSQL** (ובהמשך מפוצל) – המידע הקבוע: חשבונות, סיסמאות, דירוג, היסטוריה.
- **Kafka** – צינור אירועים בין השירותים, גרסת רשת של ה-`EventBus` שכבר יש לנו.
- **TLS + גיבוב סיסמאות** – אבטחת התעבורה והסיסמאות.
- **Prometheus / Grafana** – ניטור: לראות בזמן אמת מה עמוס ומה נפל.
- **CDN** – להגיש את הקבצים הסטטיים (תמונות כלים, קוד) קרוב לשחקן.

עכשיו נעבור על כל אחד לעומק – מה הוא, למה, ואיך מכניסים אותו למה שכבר בנינו.

---

## הבסיס: לארוז את השרת בקונטיינר (Docker)

לפני הכל, השרת צריך להפוך למשהו שאפשר להריץ ממנו הרבה עותקים זהים. היום הוא רץ
"אצלנו במחשב" עם הגרסאות שיש לנו. זה בדיוק מה שנשבר כשמריצים על עשרות מכונות.

**למה זה נחוץ:** בלי אריזה אחידה, כל מכונה מתנהגת קצת אחרת ואי אפשר לשכפל בביטחון.
קונטיינר נותן חבילה אחת שרצה אותו דבר בכל מקום.

**איך מוסיפים:** כותבים `Dockerfile` שאורז את הקוד, את Python ואת `requirements.txt`
לתמונה אחת. מרגע זה `python -m server` רץ בתוך קונטיינר, ואפשר להעלות ממנו כמה
עותקים שרוצים. שום שינוי בלוגיקת המשחק – רק אריזה סביבה.

---

## להוציא את המצב מהזיכרון החוצה (Redis)

היום השרת זוכר בזיכרון שלו מי מחובר, אילו חדרים קיימים ומי יושב בכל חדר. אם נריץ
כמה עותקים, כל אחד "יזכור" דברים אחרים והם לא ידעו אחד על השני.

**למה זה נחוץ:** כדי שכמה שרתים יוכלו לעבוד ביחד, המידע המשותף (מי מחובר, איזה חדר
על איזה שרת, תור ההמתנה) חייב לשבת במקום חיצוני משותף שכולם רואים.

**איך מוסיפים:** מוסיפים Redis כשירות נפרד, ומעבירים אליו שלושה דברים שהיום יושבים
בתוך `server/` – רשימת המחוברים, טבלת "חדר ← שרת", ותור ההמתנה למשחקים. הלוח החי
עצמו נשאר בזיכרון של שרת-המשחק (הוא חולף וזריז ממילא) – ל-Redis הולך רק מה שצריך
להיות משותף בין השרתים.

---

## מסד נתונים אמיתי במקום קובץ מקומי

מה שצריך לשרוד לנצח – חשבונות, סיסמאות, דירוג, היסטוריית משחקים – לא יכול לשבת
בקובץ על מכונה אחת.

**למה זה נחוץ:** קובץ מקומי (כמו SQLite) יודע כותב אחד בכל רגע, לא נגיש מרשת, ואם
המכונה נפלה – הכל אבד. עם מיליוני חשבונות זה פשוט לא אופציה.

**איך מוסיפים:** מקימים מסד נתונים משותף (PostgreSQL, ובהמשך מפוצל לפי משתמש) ומחברים
אליו רק שני מסלולים: מסלול ההתחברות (לאמת סיסמה) ומסלול סוף-המשחק (לכתוב תוצאה
ודירוג). שאר המערכת לא נוגעת בו – זה מה ששומר עליו רגוע. מהלך באמצע משחק אף פעם
לא כותב למסד.

**למה SQL ולא NoSQL:** הנתונים שלנו (חשבון, דירוג, תוצאה) מובנים, קשורים זה לזה,
ודורשים דיוק מוחלט – כשמשחק נגמר צריך *באותה נשימה* לכתוב תוצאה ולעדכן דירוג לשני
השחקנים, הכל-או-כלום. זה בדיוק מה ש-SQL עם ACID נותן. הנתונים גם רלציוניים: הם מפורקים
לטבלאות עם קשרים (משחק מצביע ל-user_id, לא משכפל את השם) – בלי כפילות ועם הצלבות חזקות.
לעומת זאת מצב חם וחולף (מי מחובר, תור המתנה) הולך דווקא ל-NoSQL/Redis, כי שם מהירות
חשובה יותר מדיוק. כלומר לא "SQL על פני NoSQL" אלא הכלי הנכון לכל סוג נתון.

**איך זה פותר גישה בו-זמנית:** PostgreSQL משתמש ב-MVCC (ריבוי גרסאות) – כותב יוצר
גרסה חדשה של שורה במקום לנעול אותה, כך שקריאה אף פעם לא חוסמת כתיבה ולהפך. שני משחקים
שנגמרים ומעדכנים שני משתמשים שונים נוגעים בשתי שורות שונות ורצים במקביל (נעילה ברמת
השורה, לא הטבלה). את עומס הקריאות מפזרים על Read Replicas. זו בדיוק הבעיה שהרגה את
SQLite (כותב יחיד שנועל הכל) – וכאן היא נעלמת.

---

## דלת כניסה אחת שמפזרת (Load Balancer)

כשיש הרבה שרתים, השחקן לא אמור לדעת לאיזה מהם להתחבר.

**למה זה נחוץ:** צריך גורם אחד בכניסה שמחלק כל חיבור נכנס לשרת פנוי, ומפסיק לשלוח
לשרת שנפל. אחרת שחקן "נתקע" על שרת עמוס או מת.

**איך מוסיפים:** מעמידים Load Balancer לפני קבוצת השרתים. השחקן פונה לכתובת אחת,
וההוא מחליט לאן. כדי שהוא עצמו לא יהיה צוואר בקבוק – מריצים כמה כאלה, כי הוא רק
מעביר הלאה ולא עושה עבודה כבדה.

---

## לדעת מי על איזה שרת (Room Directory)

ברגע שיש כמה שרתי-משחק, צריך לדעת באיזה מהם יושב כל חדר – כי הלוח קיים רק במקום אחד.

**למה זה נחוץ:** שני שחקנים באותו חדר חייבים להגיע לאותו שרת. בלי "ספר טלפונים"
אין דרך למצוא את השרת הנכון כשמישהו מבקש להצטרף לחדר.

**איך מוסיפים:** שומרים ב-Redis מיפוי פשוט של "חדר ← שרת", כותבים אליו כשנוצר חדר,
וכל בקשת הצטרפות מסתכלת בו ומנתבת לשרת הנכון. השרת שמחזיק את החדר מרענן את הרשומה
כל כמה שניות; אם הוא נפל, הרשומה פגה מעצמה.

---

## תור התאמה משותף (Matchmaking)

כדי ש"כולם ישחקו עם כולם" גם כשהם מפוזרים על הרבה שרתים.

**למה זה נחוץ:** אם לכל שרת יש תור משלו, שחקן תקוע לשחק רק עם מי שבמקרה על אותו
שרת. צריך תור אחד משותף שרואה את כולם.

**איך מוסיפים:** מוציאים את לוגיקת ההתאמה שהיום בתוך השרת לשירות נפרד קטן שמסתמך על
תור ב-Redis. הוא מזווג שני שחקנים, בוחר שרת פנוי, פותח שם חדר וכותב אותם ל-Directory.
כדי שהתור לא ייחנק, מפצלים אותו לפי דירוג ואזור – ממילא רוצים לזווג שחקנים ברמה דומה.

---

## מנצח על התזמורת (Kubernetes)

עם עשרות ומאות קונטיינרים שעולים ונופלים, אי אפשר לנהל אותם ביד.

**למה זה נחוץ:** צריך גורם שמחליט על איזו מכונה להריץ כל קונטיינר, מריץ מחדש מה
שקרס, ומוסיף/מוריד עותקים לפי העומס. בלי זה, כל תקלה דורשת אדם.

**איך מוסיפים:** אחרי שהשרת ארוז בקונטיינר, מגדירים אותו כשירות ב-Kubernetes ונותנים
לו כלל פשוט: "כשיש הרבה חדרים פעילים – הרץ עוד שרתי-משחק; כשיש מעט – כבה". ככה
המערכת מתאימה את עצמה לכל כמות, ומשלמים רק על מה שרץ עכשיו.

---

## צינור אירועים בין השירותים (Kafka)

כשמשחק נגמר, כמה גורמים רוצים לדעת – הדירוג, ההיסטוריה, האנליטיקה. שרת-המשחק לא
אמור להכיר את כולם בשם.

**למה זה נחוץ:** אם כל שירות קורא לכל שירות אחר ישירות, נוצר סבך שנשבר בקלות. עדיף
שכל אחד "יפרסם" מה קרה, ומי שרוצה יקשיב.

**איך מוסיפים:** זה בעצם ה-`EventBus` שכבר יש לנו בקוד – רק מוגדל לרמת רשת. במקום
לפרסם אירועים בתוך התהליך, שרת-המשחק שולח את `GameEnded` ל-Kafka, וכל שירות
מעוניין נרשם בעצמו. להוסיף מחר עוד מאזין (למשל מערכת הישגים) לא דורש לגעת בשרת
בכלל. בונוס: אם שירות אחד עמוס, Kafka מחזיק את האירועים בטור עד שיתפנה, בלי לאבד כלום.

---

## התוספות שבלעדיהן זה נשבר בעולם האמיתי

עד כאן זה השלד. הדברים הבאים הם מה ש"אף אחד לא מבקש עד שהמערכת נופלת", והם ההבדל
בין הדגמה שרצה פעם לבין שירות אמיתי:

- **ניטור (Prometheus/Grafana):** עם מאות קונטיינרים אי אפשר להסתכל בלוג. צריך לוחות
  שמראים בזמן אמת מה עמוס ומה נפל. *מוסיפים:* כל שרת חושף כמה מספרים (חדרים פעילים,
  זמן תגובה), ומערכת ניטור אוספת אותם.

- **בדיקות בריאות (Health Checks):** Kubernetes צריך לדעת אם קונטיינר בריא כדי להחליף
  אותו. *מוסיפים:* נקודת קצה קטנה בשרת שעונה "אני חי".

- **הגבלת קצב והגנה מפני שיטפון (Rate Limiting):** לחסום שחקן או בוט ששולח אלפי
  בקשות בשנייה. *מוסיפים:* בדיקה ב-Load Balancer או בשער שסופרת בקשות לכל משתמש.

- **אבטחה:** הצפנת תעבורה (TLS) וגיבוב סיסמאות – סיסמה לעולם לא נשמרת כטקסט גלוי.
  *מוסיפים:* מפעילים TLS בכניסה, ובמסלול ההתחברות שומרים רק גיבוב (bcrypt/Argon2).

- **חזרה לאחר ניתוק (Reconnect):** שחקן שהאינטרנט שלו קפץ צריך לחזור לאותו חדר. יש לנו
  כבר בסיס לזה (disconnect-resign). *מוסיפים:* מזהה סשן קצר שמאפשר להתחבר חזרה לחדר הקיים.

- **מניעת כפילויות (Idempotency):** ברשת הודעה יכולה להגיע פעמיים; מהלך לא צריך
  להתבצע פעמיים. *מוסיפים:* מזהה ייחודי לכל פקודה, והשרת מתעלם ממזהה שכבר ראה.

- **פריסת גרסאות חלקה (CI/CD):** לעדכן גרסה בלי להפיל את המשחק לאף אחד. *מוסיפים:*
  Kubernetes מחליף קונטיינרים בהדרגה, עם אפשרות לחזור אחורה אם משהו נשבר.

- **CDN:** להגיש את קבצי המשחק (תמונות כלים, קוד) קרוב לשחקן גאוגרפית. *מוסיפים:* מעלים
  את הקבצים הסטטיים לרשת הפצה, כדי שהשרת יתעסק רק במשחק עצמו.

---

## תעבורת הרשת – מה שולחים על החוט (ומה לשפר)

זה נושא שבדקנו בקוד עצמו, ושווה לתעד כי הוא משפיע ישירות על ה-scale.

**המצב היום:** השרת פועם ~30 פעם בשנייה (`SERVER_TICK_MS = 33`), ובכל טיק `room.tick`
משדר את **המצב המלא** (כל הכלים + כל יומן המהלכים) לכל חבר בחדר. זה עיצוב Full Snapshot
– פשוט וחסין (לקוח שמפספס עדכון מתוקן על ידי הבא), אבל בזבזני: ~3–4 קילובייט 30 פעם
בשנייה לכל שחקן (~1 Mbit/s), כשבתיאוריה מהלך שוקל ~60 בייט. הפער מגיע לפי אלפים ב-scale.

יש שלוש רמות של שיפור, מהפשוט למתוחכם:

- **Full Snapshot (היום):** שולחים הכל כל פעם. פשוט, אך בזבזני.
- **Delta:** שולחים רק את ההפרש מהמצב הקודם ("הכלי מ-e2 עבר ל-e4"). הלקוח מדביק את
  השינוי על מצב שהוא כבר מחזיק. חוסך מ-4 קילובייט לעשרות בייט.
- **Event-Driven + Client-Side Interpolation:** שולחים את ה**כוונה** פעם אחת ("כלי יצא
  מ-A ל-B בזמן T, משך D"), והלקוח מחשב בעצמו את כל פריימי האנימציה. זו הרמה הכי חזקה,
  והיא בדיוק מה שה-`EventBus` שלנו כבר בנוי לאפשר.

**מה מומלץ אצלנו (מדורג):**

1. לשדר רק על שינוי אמיתי (dirty flag), לא בכל טיק – החיסכון הכי גדול ומיידי.
2. לעבור ל-Event-Driven + אנימציה בצד הלקוח, במקום לשדר progress/cooldown 30 פעם בשנייה.
3. לא לשלוח את כל יומן המהלכים כל פעם – רק מהלך חדש (הוא כבר עובר גם כ-event).
4. לנתק את קצב השידור מקצב הטיק (לפעום ב-30Hz, לשדר ב-10Hz או על-שינוי).
5. דחיסה (permessage-deflate) וקידוד קומפקטי – ניצחון כמעט חינם, משני לשאר.

---

## חלוקת השרתים – לוגית ופיזית (זה דורש תכנון!)

"לחלק שרתים" זה בעצם שני דברים נפרדים, וכדאי לתכנן כל אחד בנפרד.

### חלוקה לוגית – לפי מה מחלקים את העבודה

זו שאלת התוכן: איזה שחקן או חדר שייך לאיזה שרת. המפתח משתנה לפי מה שהקוד שואל:

- **שרתי-משחק → לפי חדר בגיבוב (hash).** זהות שרת-משחק היא "מי מחזיק את החדר", אז
  מחלקים לפי `hash(מספר_חדר)`. שני שחקנים באותו חדר מקבלים אותו hash ולכן אותו שרת.
  משתמשים ב-Consistent Hashing כדי שהוספת/הסרת שרת תזיז רק חלק קטן מהמיפוי.
- **תור ההתאמה → לפי דירוג ואזור.** מפצלים את התור לפי טווחי ELO (כי ממילא רוצים
  לזווג רמות דומות) ולפי אזור גאוגרפי – גם משפר את המשחק וגם מפזר עומס.
- **המסד → לפי `user_id`.** מפצלים את המשתמשים לפי גיבוב של המזהה, כדי לפזר שווה.

הרעיון: בכל מקום בוחרים מפתח חלוקה שגם מפזר שווה וגם מתאים לשאלה שהקוד שואל.

### חלוקה פיזית – על איזו חומרה זה יושב

זו שאלת התשתית, ונפרדת מהחלוקה הלוגית (אותו שרת לוגי יכול לזוז בין מכונות):

- **גאוגרפית** – מרכזי נתונים בכמה אזורים בעולם, כדי שכל שחקן ידבר עם השרת הקרוב.
- **צפיפות** – על מכונה פיזית חזקה רצים הרבה קונטיינרים קלים ביחד.
- **בידוד תקלות** – לא שמים את כל העותקים של שירות על אותה מכונה.

הבחנה חשובה: משחק חי **אף פעם לא עובר** בין שרתים – הוא מסיים את 30–90 השניות שלו
במקום. ה-Consistent Hashing משנה רק לאן ילכו חדרים **חדשים**; הישנים נשרפים מעצמם
תוך פחות מדקה וחצי, וכך הכל מתאזן בלי להעביר אף משחק חי.

### מה לתכנן ב-Kubernetes

Kubernetes לא עובד לבד – נותנים לו כללים והוא מבצע. מה שצריך להגדיר:

- **מינימום/מקסימום עותקים** לכל סוג שירות (שרת-משחק: מ-5 עד אלפים; auth: מ-2 עד עשרות).
- **מדד ההתרחבות** – אצלנו לפי **מספר החדרים הפעילים**, לא לפי CPU, כי זה מה שבאמת מודד עומס.
- **בדיקת בריאות** – נקודת קצה שעונה "אני חי", כדי ש-Kubernetes יחליף מה שלא עונה.
- **משאבים לכל קונטיינר** – כמה CPU/זיכרון, כדי לדעת כמה להכניס על כל מכונה.
- **ניתוב דביק** – ששחקן יישאר עם שרת החדר שלו לאורך כל המשחק.

הבחנה אחרונה: *סוגי קונטיינרים* = כמה תפקידים יש (אצלנו ~4). *מספר השרתים* = כמה
מופעים רצים בפועל מכל סוג – וזה משתנה כל הזמן עם העומס. סוג אחד יכול להיות מאה שרתים.

---

## סדר העבודה המומלץ

לא עושים הכל ביום אחד. סדר הגיוני שכל שלב בו עומד בפני עצמו, ומה כל שלב דורש בפועל:

לכל שלב: **(דורש)** · **(שינויי קוד מדויקים)** · **(קושי)**. עקרון-על: ה-DI,
ה-`EventBus` וה-`GameGateway` הקיימים הופכים חלק מהשלבים ל-easy; פיצול החדרים
ה-stateful בין תהליכים הוא ה-hard.

**0. Docker + חילוץ הקונפיג.** *דורש:* `Dockerfile`, `docker-compose.yml`, env.
*קוד:* `config/settings.py` – להחליף `SERVER_HOST/PORT`, `ACCOUNTS_DB` בקריאה מ-`os.environ`
עם ברירת-מחדל; `build_service(config=...)` כבר מזריק קונפיג. *קושי:* **easy** – אריזה בלבד.

**1. הוצאת ה-state המשותף ל-Redis.** *דורש:* Redis + client. *קוד:* שלושת ה-singletons –
`lobby.py` (`_rooms` dict + `_next_id` → **Room Directory** ב-Redis עם מזהה גלובלי; מודול חדש
`server/room_directory.py`), `matchmaking.py` (`_waiting` list → sorted-set ב-Redis), presence
של `registry.py` ל-Redis. הלוח החי לא יוצא מה-RAM. *קושי:* **hard** – שבירת ההנחה "תהליך אחד זוכר הכל".

**2. PostgreSQL במקום SQLite.** *דורש:* Postgres + asyncpg. *קוד:* מודול חדש
`accounts/postgres_store.py` באותו contract; `build_service(store=...)` **כבר** מזריק – רק מחליפים
ברירת-מחדל. *קושי:* **easy מבנית / hard תפעולית** – ה-`commit()` היום **חוסם** את ה-event loop; חובה
async/thread-pool. נוגעים רק ב-login וב-`server/ratings.py`.

**3. API Gateway (HTTP).** *דורש:* שירות HTTP (FastAPI): login/rooms/history. *קוד:* להוציא את
`GameService.admit()` ל-endpoint (כבר לא-stateful), מנפיק session-token ל-Redis. *קושי:* **easy**.

**4. WebSocket Gateway נפרד מ-Game Server.** *דורש:* הפרדת שכבת החיבורים משכבת ה-state. *קוד:*
`server/socket.py` (`_clients` + `_pump` שהוא ה-tick היחיד) – ה-Gateway מאמת מול Redis-session ומנתב
את הסוקט ל-shard לפי ה-Directory; `server/outbox.py` → fan-out דרך PubSub. *קושי:* **hard** – "shard
יחיד מחזיק את השעון של החדר", סטיקיות ברמת-חדר.

**5. Game Allocator + Consistent Hashing.** *קוד:* מודול חדש `server/allocator.py` עם hash-ring על
ה-shards החיים; מחליף את `Lobby.create()`. משחק חי **לא נודד**. *קושי:* **easy יחסית** – `create()` כבר
נקודת-הזרקה אחת.

**6. Matchmaker כשירות.** *קוד:* `matchmaking.py` יוצא כשירות; `seek/cancel/tick` נשארים אבל התור
ב-Redis, וכשמוצא זוג קורא ל-Allocator. פיצול לפי ELO/אזור. *קושי:* **easy**.

**7. NATS (מהיר) + Kafka (עמיד) במקום EventBus הפנימי.** *קוד:* מימוש רשת חדש `events/network_bus.py`
באותו interface; `build_room` מחליף את ה-`EventBus()` המוזרק; `GameEnded` → Kafka, ו-`ratings`/persistence
כ-consumers. *קושי:* **easy** – היתרון הארכיטקטוני הגדול.

**8. Observability + Kubernetes/K3s.** *קוד:* health/metrics endpoints; autoscale על **חדרים פעילים**
(לא CPU); manifests לכל סוג שירות. *קושי:* **easy** – עוטף, לא משנה לוגיקה.

**תיקונים חוצי-שלבים (קדם-scale, לקפל פנימה):** reconnect-with-resume (`room.py::leave`+`join`,
session-token); idempotency keys (מזהה-פקודה ב-`protocol/messages.py`, dedup ב-`handler.py`);
rate limiting (מונה per-user בשער).

**קבצים קריטיים למימוש:** `server/__main__.py` (נקודת ה-DI), `server/lobby.py` (הפיצול המרכזי),
`server/socket.py` (לולאת ה-tick), `server/matchmaking.py` (התור), `accounts/sqlite_store.py` (ה-commit החוסם).

> הערה לגבי הסקיילינג של המסד: מתחילים ב-PostgreSQL פשוט. רק כשמגיעים לתקרת
> הכתיבה של מכונה אחת עוברים ל-SQL מבוזר (CockroachDB/Spanner) – אותן טבלאות ואותו
> SQL, רק מנוע שגדל לרוחב לבד. מעבר קל דווקא כי שמרנו על SQL רגיל מההתחלה.

הרעיון היפה הוא שהרבה מהתשתית כבר קיימת אצלנו במחשבה הנכונה: הפרדת השכבות, ה-EventBus,
ה-Gateway שמסתיר מאיפה מגיע המשחק. המעבר להרבה משתמשים הוא פחות "לכתוב הכל מחדש"
ויותר "לקחת את אותם עקרונות ולפזר אותם על הרבה מכונות".

---

## הארכיטקטורה הסופית (Target) – מלאי הרכיבים

זהו המיזוג של התכנון שלנו עם ההצעה שהוגשה. הכלל נשמר: כל "אחד" נשבר להרבה זהים,
וה-`GameEngine` הוא הפוסק היחיד – הלקוח והשערים לעולם לא מכריעים חוקים.

**שכבת כניסה ותקשורת**

| רכיב | תפקיד | מצב | טכנולוגיה |
|---|---|---|---|
| Load Balancer | מפזר כל חיבור נכנס; יושב לפני *שני* השערים | Stateless | Cloud LB / nginx |
| API Gateway | HTTP: התחברות, רשימת חדרים, היסטוריה | Stateless | HTTP service + TLS |
| WebSocket Gateway | מחזיק חיבורים חיים, מזרים פקודות ומצב | Stateful (חיבורים) | WebSocket service |
| CDN | הגשת קבצים סטטיים קרוב לשחקן | Stateless (cache) | CDN |

**שכבת הקצאה ומשחק**

| רכיב | תפקיד | מצב | טכנולוגיה |
|---|---|---|---|
| Matchmaking | מזווג שחקנים לפי ELO/אזור, פותח חדר | Stateless (תור ב-Redis) | שירות ייעודי |
| Game Allocator | מחליט איזה Game Server מארח כל חדר | Stateless (מיפוי ב-Redis) | Consistent Hashing |
| Room Directory | ספר טלפונים "חדר ← שרת" | Stateful | Redis (TTL) |
| Game Server Shards | מריץ את ה-`GameEngine`, מחזיק את הלוח החי | Stateful (RAM) | קונטיינר Python (הקוד הקיים) |

**שכבת מצב ונתונים**

| רכיב | תפקיד | מצב | טכנולוגיה |
|---|---|---|---|
| Shared State | מי מחובר, מיפוי חדרים, תור המתנה | Stateful | Redis |
| Internal PubSub | תיאום פנימי מהיר וחולף בין השירותים | transport | NATS / Redis PubSub |
| Durable Event Stream | אירועים שאסור לאבד (`GameEnded`→דירוג/היסטוריה) | Stateful (log) | Kafka |
| Persistent DB | חשבונות, דירוג, היסטוריה | Stateful | PostgreSQL ← (בקצה: CockroachDB) |

**שכבת תשתית, תפעול ואבטחה**

| רכיב | תפקיד | טכנולוגיה |
|---|---|---|
| Containerization | אריזת כל שירות | Docker (מקומי: Docker Compose) |
| Orchestration | הרצה/ריפוי/הרחבה לפי חדרים פעילים | Kubernetes (קל: K3s) |
| Observability | ניטור, לוחות, ובדיקות עומס (load tests) | Prometheus / Grafana + k6 |
| Security | הצפנת תעבורה + גיבוב סיסמאות | TLS + bcrypt/Argon2 |
| Resilience Guards | reconnect-with-resume, idempotency, rate limiting | לוגיקה בשערים + Redis |

---

## כמה קונטיינרים ושרתים מכל סוג (קיבולת בשיא)

תרחיש להמחשה: ~10M משחקים פעילים בו-זמנית. **סוגי קונטיינרים** = ~11 תפקידים;
**מספר שרתים** = אלפי instances, כמעט כולם מרוכזים בשני תפקידים.

| שירות | מצב | # בשיא | מדד התרחבות | למה |
|---|---|---|---|---|
| Load Balancer | stateless | 2–8 | חיבורים/שנייה | קצה מנוהל, מפזר לאזורים |
| API Gateway | stateless | ~50–100 | בקשות/שנייה | login/lobby בלבד, קליל |
| WebSocket Gateway | stateless* | **~1,000–2,000** | **מספר חיבורים פתוחים** | ~10K חיבורים ל-pod → 10M/10K |
| Matchmaker | stateless | ~20–50 | עומק התור | זיווג מהיר, נשען על Redis |
| Game Allocator | stateless | ~10–30 | קצב הקצאות | control-plane, נפח נמוך |
| **Game Server Shards** | **stateful** | **~2,500** | **מספר חדרים פעילים** | 10M ÷ 4,000 חדרים לשרת |
| Redis (cluster) | stateful | ~50–100 | זיכרון/ops | תורים, sessions, presence |
| PostgreSQL | stateful | 1 primary + ~5–15 replicas | read QPS / lag | קבוע, מחוץ למסלול החם |
| NATS | stateful-ish | ~10–30 | msg/s | תיאום פנימי מהיר |
| Kafka | stateful | ~20–50 brokers | ingest / lag | log עמיד לאירועים |
| Observability | stateful | ~30–80 | נפח מדדים | Prometheus/Loki/traces |

**התובנה:** ~11 *סוגים*, אבל הצי (אלפי instances) מרוכז בשניים בלבד – **WebSocket
Gateway** (מתרחב על חיבורים) ו-**Game Server Shards** (מתרחב על חדרים פעילים). כל השאר
בעשרות. **סוגים ≠ מספר שרתים.**

**מציאות רוחב-הפס:** ~מהלך כל 2 שניות × תוכן זעיר על פני 10M ≈ **~5 Gbit/s בסך הכל** –
זניח לרשת. הצוואר האמיתי הוא **מספר החיבורים הפתוחים** ו-**CPU של השעונים החיים**, לא התעבורה.

---

## החלטות טכנולוגיה – מה להוסיף מול ההצעה שהוגשה

- **NATS *וגם* Kafka – שניהם.** NATS/Redis PubSub לתיאום פנימי מהיר וחולף (presence,
  ניתוב); Kafka לזרם אירועים עמיד שאסור לאבד (`GameEnded`→דירוג/היסטוריה). זו גרסת-הרשת
  של ה-`EventBus` הקיים.
- **פיצול השער לשניים** (API מול WebSocket) – עומסים שונים שמתרחבים בנפרד, עם Load
  Balancer יחיד לפני שניהם.
- **Game Allocator מול Matchmaker** – ה-Matchmaker מחליט *עם מי*; ה-Allocator *איפה*.
- **PostgreSQL עכשיו, CockroachDB רק בקצה** – אותו SQL, מנוע שגדל לרוחב, רק בפגיעה בתקרה.
- **Docker Compose (מקומי) ו-K3s (קל)** – אותה ארכיטקטורה בשני גדלים.
- **Load testing** (k6/locust) – להוכיח שה-scale עובד, לא רק למדוד.

## מה אסור לאבד במיגרציה – Server Authority

- **פוסק יחיד:** `handler._owns()` קורא את הכלי מהלוח, לא ממה שהלקוח טען; אין
  Client-Side Prediction/Optimistic – מותרים רק interpolation, debounce, reconnect.
- **שעון יחיד לחדר:** בפיצול לשרתים, אסור ששני shards יריצו `tick` על אותו חדר.
- **הלוח החי נשאר ב-RAM:** מסלול-המהלך לעולם לא נוגע ב-DB; רק תוצאת סוף-משחק נכתבת.
- **בידוד per-room:** כל חדר עם bus משלו – לשמר גם מעל PubSub (topic per-room).
- **חוזה ה-`GameGateway`:** ה-UI מדבר רק מולו; אסור להדליף ידע-רשת ל-`client/`.

---

## פערים ידועים – ומה מהם באמת קשור ל-scale

עברנו על הקוד ומצאנו כמה בעיות. חשוב להפריד בין שתיים: בעיות שהמעבר ל-scale נועד
לפתור, לעומת בעיות שקיימות כבר היום (עם שני שחקנים) וה-scale לא נוגע בהן.

### נוצרות בגלל ה-scale – רדומות היום, נשברות בגודל

- **הכל singletons בזיכרון** (Lobby, Matchmaker, מזהי חדרים). על שרת אחד עובד; שובר
  ריצה של כמה instances. **נפתר** בצעדי Redis + Room Directory + שירות matchmaking.
- **SQLite עושה `commit()` חוסם על ה-event loop** – בעומס גבוה מקפיא את כל המשחקים.
  **נפתר חלקית** במעבר ל-PostgreSQL, אך דורש גם async driver / thread pool כדי לא לחסום.
- **שידור מצב מלא בכל טיק** – בזבזני, אך מזיק רק בקנה מידה. דורש שינוי transport
  (ראו הסעיף על תעבורת הרשת).

### קיימות כבר עכשיו – קדם-scale, דורשות תיקון ייעודי

- **באג ב-reconnect:** שחקן שמתנתק לרגע וחוזר נכנס כצופה וה-countdown ממשיך – אז הוא
  מפסיד את המשחק, בניגוד לכוונה המוצהרת. קורה היום עם שני שחקנים. תיקון: לתת לחוזר
  לקבל בחזרה את הצבע שלו ולבטל את ה-countdown.
- **אין interpolation בלקוח:** האנימציה מקרטעת כי הלקוח מצייר 60fps ומקבל 30Hz. פתרון
  UX בצד הלקוח – מחשב מיקומי ביניים בעצמו (לא prediction, רק ריכוך של מצב שכבר אושר).
- **אין debounce על מהלכים:** לחיצות מהירות מציפות את השרת. ה-hints כבר עוברים debounce
  – כדאי אותו דבר למהלכים.

### עיקרון שמנחה את כל אלה

השרת הוא הפוסק היחיד (Server Authority): הלקוח לעולם לא מבצע פעולה שלא אושרה. לכן
פוסלים Client-Side Prediction ו-Optimistic moves – ומשאירים רק שיפורים שלא ממציאים
פעולה: interpolation (ריכוך), debounce (מניעת כפילות), ו-reconnect (החזרת מצב קיים).
